// Generated by CoffeeScript 1.10.0
(function() {
  var MenuItem, Separator,
    slice = [].slice;

  Separator = (function() {
    function Separator() {
      this.type = 'separator';
    }

    return Separator;

  })();

  MenuItem = (function() {
    function MenuItem(label, accelerator, roleOrClick, submenu, role) {
      this.label = label;
      if ((submenu != null) && submenu === true) {
        this.submenu = [];
      }
      if (accelerator != null) {
        this.accelerator = accelerator;
      }
      if (roleOrClick != null) {
        if (typeof roleOrClick === 'function') {
          this.click = roleOrClick;
        }
        if (typeof roleOrClick === 'string') {
          this.role = roleOrClick;
        }
      }
      if (role != null) {
        this.role = role;
      }
    }

    MenuItem.prototype.prependChild = function(item) {
      return this.submenu.unshift(item);
    };

    MenuItem.prototype.appendChild = function(item) {
      return this.submenu.push(item);
    };

    MenuItem.prototype.appendChildren = function() {
      var items;
      items = 1 <= arguments.length ? slice.call(arguments, 0) : [];
      return this.submenu.push(items);
    };

    return MenuItem;

  })();

  module.exports = {
    Separator: Separator,
    MenuItem: MenuItem
  };

}).call(this);

//# sourceMappingURL=menus.js.map
